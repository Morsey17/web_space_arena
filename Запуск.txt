docker build -t flask-game-ssl2:latest .

docker save -o flask-game-ssl2.tar flask-game-ssl2:latest

scp flask-game-ssl2.tar root@tss-server.ru:/root/

ssh root@tss-server.ru

docker load -i /root/flask-game-ssl2.tar

docker run -d \
  --name flask-game-ssl2 \
  -p 5000:5000 \
  --restart always \
  flask-game-ssl2:latest

docker logs flask-game --tail 20